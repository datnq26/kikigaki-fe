export interface LessonResponse {
    id: number
    title: string
    content: string
    audio_url: string
    image: string
    duration_minutes: number
    access_level: string
    course: string
}
